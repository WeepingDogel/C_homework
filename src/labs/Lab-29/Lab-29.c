#include <stdio.h>
#include <stdlib.h>

// Node structure
struct Node {
    int data;
    struct Node* left;
    struct Node* right;
};

// Function to count nodes
int countNodes(struct Node* root) {
    // TODO: Implement node counting logic
}

int main() {
    // TODO: Build a binary tree and call countNodes()
    return 0;
}
