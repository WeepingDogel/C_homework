#include <stdio.h>

// Recursive function to calculate GCD
int gcd(int a, int b) {
    // TODO: Implement GCD logic
}

int main() {
    int a, b;

    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);

    printf("GCD of %d and %d is %d\n", a, b, gcd(a, b));

    return 0;
}
