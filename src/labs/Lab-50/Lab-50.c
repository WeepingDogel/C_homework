#include <stdio.h>
#include <string.h>

void countCharacterFrequency(char* str) {
    // TODO: Implement logic to count frequency of characters
}

int main() {
    char str[100];
    printf("Enter a string: ");
    scanf("%s", str);

    countCharacterFrequency(str);

    return 0;
}
