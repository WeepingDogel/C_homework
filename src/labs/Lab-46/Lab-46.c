#include <stdio.h>
#include <stdlib.h>

// Define Node structure
struct Node {
    int data;
    struct Node* left;
    struct Node* right;
};

int calculateHeight(struct Node* root) {
    // TODO: Implement height calculation logic
}

int main() {
    struct Node* root = NULL;
    // TODO: Build a binary tree and call calculateHeight()

    return 0;
}
