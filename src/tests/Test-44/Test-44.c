#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

// Define Node structure
struct Node {
    int data;
    struct Node* left;
    struct Node* right;
};

int maxPathSumUtil(struct Node* root, int* res) {
    // TODO: Implement utility function to calculate maximum path sum
}

int maxPathSum(struct Node* root) {
    // TODO: Implement main function to find maximum path sum
}

int main() {
    struct Node* root = NULL;
    // TODO: Build a binary tree and call maxPathSum()

    return 0;
}
