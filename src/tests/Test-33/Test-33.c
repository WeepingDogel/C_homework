#include <stdio.h>
#include <stdlib.h>

// Define Node structure
struct Node {
    int data;
    struct Node* left;
    struct Node* right;
};

// Function to calculate the maximum depth
int maxDepth(struct Node* root) {
    // TODO: Implement recursive logic to find maximum depth
}

int main() {
    struct Node* root = NULL;
    // TODO: Build a binary tree and call maxDepth()

    return 0;
}
