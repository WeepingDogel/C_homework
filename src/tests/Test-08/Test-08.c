#include <stdio.h>
#include <stdlib.h>

// Define Node structure
struct Node {
    int data;
    struct Node* left;
    struct Node* right;
};

// Function to find the LCA
struct Node* findLCA(struct Node* root, int n1, int n2) {
    // TODO: Implement logic to find LCA
}

int main() {
    struct Node* root = NULL;
    // TODO: Build a binary tree and call findLCA()

    return 0;
}
