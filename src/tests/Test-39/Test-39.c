#include <stdio.h>
#include <string.h>

void computeZArray(char* str, int Z[]) {
    // TODO: Implement Z-array computation
}

void ZAlgorithm(char* text, char* pattern) {
    // TODO: Implement Z-Algorithm for pattern matching
}

int main() {
    char text[100], pattern[100];
    printf("Enter the text: ");
    scanf("%s", text);
    printf("Enter the pattern: ");
    scanf("%s", pattern);

    ZAlgorithm(text, pattern);

    return 0;
}
