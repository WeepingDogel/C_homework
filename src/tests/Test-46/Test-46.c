#include <stdio.h>
#include <stdlib.h>

// Define Node structure
struct Node {
    int data;
    struct Node* left;
    struct Node* right;
};

void flatten(struct Node* root) {
    // TODO: Implement logic to flatten the binary tree
}

int main() {
    struct Node* root = NULL;
    // TODO: Build a binary tree and call flatten()

    return 0;
}
