#include <stdio.h>

int maxProfit(int prices[], int size) {
    // TODO: Implement logic to find maximum profit
}

int main() {
    int prices[] = {7, 1, 5, 3, 6, 4};
    int size = sizeof(prices) / sizeof(prices[0]);

    printf("Maximum Profit: %d\n", maxProfit(prices, size));

    return 0;
}
