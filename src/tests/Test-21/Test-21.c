#include <stdio.h>
#include <stdlib.h>

// Define Node structure
struct Node {
    int data;
    struct Node* left;
    struct Node* right;
};

// Function to count leaf nodes
int countLeafNodes(struct Node* root) {
    // TODO: Implement logic to count leaf nodes
}

// Helper functions to create a new node and build the tree
struct Node* createNode(int data) {
    // TODO: Implement logic to create a new node
}

int main() {
    struct Node* root = NULL;
    // TODO: Build a binary tree and call countLeafNodes()

    return 0;
}
