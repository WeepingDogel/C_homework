#include <stdio.h>
#include <string.h>

int countDistinctSubstrings(char* str) {
    // TODO: Implement logic to count distinct substrings
}

int main() {
    char str[100];
    printf("Enter a string: ");
    scanf("%s", str);

    printf("Number of distinct substrings: %d\n", countDistinctSubstrings(str));

    return 0;
}
